export type ApiDataType = {
  hdurl: string;
  date: string;
  copyright: string;
  explanation: string;
  media_type: string;
  service_version: string;
  title: string;
  url: string;
};
export type Status = "loading" | "error" | "success" | "idle";
